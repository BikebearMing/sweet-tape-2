node_modules/
.next/
out/
build/

.env
.env.local
.env*.local

# Payload writes uploads here at runtime
public/media/

next-env.d.ts
*.tsbuildinfo

.DS_Store

# One-time migration artefacts: the path→media-id map and the pre-migration
# snapshot of the tapes. Kept on disk as a safety net, not history.
scripts/.tape-backup.json
scripts/.tape-media-map.json
